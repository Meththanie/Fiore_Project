<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Reset Password | FIORE</title>
  <style>
    * {
      box-sizing: border-box;
    }

    .navbar {
  pointer-events: none;
  user-select: none;
  opacity: 0.8; /* Optional: gives a subtle faded effect */
}
    body {
      margin: 0;
      padding: 0;
      font-family: 'Poppins', sans-serif;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      background: url('/img/bg-login.jpg') no-repeat center center fixed;
      background-size: cover;
      position: relative;
    }

    body::before {
      content: '';
      position: absolute;
      top: 0; 
      left: 0; 
      width: 100%; 
      height: 100%;
      background: rgba(249, 246, 242, 0.6); /* Soft luxury overlay */
      z-index: 0;
    }

    main {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      padding-top: 80px;
      position: relative;
      z-index: 1;
    }

    .reset-container {
      padding: 40px 30px;
      max-width: 420px;
      width: 100%;
      background: rgba(255, 255, 255, 0.92);
      border-radius: 18px;
      box-shadow: 0 12px 24px rgba(0, 0, 0, 0.08);
      backdrop-filter: blur(10px);
      border: 1px solid #e0c68d;
    }

    .reset-container h1 {
      margin-bottom: 30px;
      font-size: 32px;
      color: #1a1a1a;
      font-weight: 600;
      text-align: center;
    }

    .reset-container input {
      width: 100%;
      padding: 14px 18px;
      margin: 10px 0;
      border: 1px solid #d4af37;
      border-radius: 12px;
      background: rgba(255, 255, 255, 0.8);
      font-size: 15px;
      outline: none;
      transition: background 0.3s ease, border-color 0.3s ease;
    }

    .reset-container input:focus {
      background: rgba(255, 255, 255, 1);
      border-color: #b88a00;
    }

    .reset-container button {
      width: 100%;
      padding: 14px;
      margin-top: 20px;
      border: none;
      background: linear-gradient(135deg, #b88a00, #d4af37);
      color: white;
      font-size: 16px;
      font-weight: 600;
      border-radius: 12px;
      cursor: pointer;
      transition: transform 0.3s ease, background 0.3s ease;
      letter-spacing: 0.5px;
    }

    .reset-container button:hover {
      transform: translateY(-2px);
      background: linear-gradient(135deg, #a17900, #cfa935);
    }

    .reset-container p {
      margin-top: 18px;
      font-size: 14px;
      color: #4b4b4b;
      text-align: center;
    }

    .reset-container a {
      color: #b88a00;
      text-decoration: none;
      font-weight: 500;
    }

    .reset-container a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

<main>
  <div class="reset-container">
    <h1>Reset Password</h1>

    <form action="/reset_password/<%= token %>" method="POST">
      <input type="password" name="password" placeholder="Enter new password" required>
      <button type="submit">Reset Password</button>
    </form>

    <p><a href="/login">Back to login</a></p>
  </div>
</main>

</body>
</html>
